export async function storeFunnelOutcome(params: {
  campaignId: string;
  outcome: string;
  recordedAt: string;
}): Promise<void> {
  void params.campaignId;
  void params.outcome;
  void params.recordedAt;
}
